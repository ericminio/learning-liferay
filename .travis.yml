language: java
jdk: openjdk8
services:
  - docker
addons:
  firefox: latest
before_install:
  - echo $TRAVIS_BUILD_DIR
  - docker run --rm hello-world
before_script:
  - docker-compose up -d postgres mysql
  - chmod +x ./wait.sh
  - ./wait.sh
script:
  - java -version
  - docker ps -a
