version: '3.4'

services:
    postgres:
      image: postgres
      container_name: postgres
      ports:
        - 2345:5432
      environment:
        - POSTGRES_USER=liferay
        - POSTGRES_PASSWORD=liferay
